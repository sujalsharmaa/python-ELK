input {
  beats {
    port => 5044
  }
}

filter {
  json {
    source => "message"
  }
}

output {
  elasticsearch {
    hosts => ["http://44.212.46.83:9200"]
    index => "flask-logs"
  }
  stdout { codec => rubydebug }
}
