input {
  tcp {
    port => 5044
    codec => json
  }
}

filter {
  json {
    source => "message"
  }
}

output {
  elasticsearch {
    hosts => ["http://98.80.67.187:9200"]
    index => "flask-logs"
  }
  stdout { codec => rubydebug }
}
