input {
  beats {
    port => 5044
  }
}

filter {
  json {
    source => "message"
  }
}

output {
  elasticsearch {
    hosts => ["http://98.80.67.187:9200"]
    index => "flask-logs"
  }
  stdout { codec => rubydebug }
}
