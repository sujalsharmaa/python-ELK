filebeat.inputs:
  - type: log
    enabled: true
    paths:
      - /app/app.log  # Correct path

output.logstash:
  hosts: ["http://44.205.11.184:5044"]

setup.kibana:
  host: "http://44.205.11.184:5601"

monitoring.enabled: true


logging:
  level: info
  to_files: true
  files:
    path: "/var/log/filebeat"
    name: filebeat
    keepfiles: 7
    permissions: 0644
